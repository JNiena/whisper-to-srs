### A script to convert output from [OpenAI's Whisper](https://github.com/openai/whisper) to SRS format.

Enter data into `whisper.txt` and run with `python whisper.py`. The script will output to `whisper.srs`.
